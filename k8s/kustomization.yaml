apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: credit-scoring-engine-prod
  labels:
    app.kubernetes.io/name: credit-scoring-engine
    app.kubernetes.io/version: "3.1.0"
    app.kubernetes.io/part-of: banking-platform
    environment: prod
    managed-by: kubernetes

resources:
- deployment.yaml
- service.yaml
- configmap.yaml
- fluent-bit-sidecar.yaml

commonLabels:
  app.kubernetes.io/name: credit-scoring-engine
  app.kubernetes.io/version: "3.1.0"
  app.kubernetes.io/part-of: banking-platform
  environment: prod
  managed-by: kubernetes

namePrefix: banking-
nameSuffix: -prod

images:
- name: registry.bank.internal/credit-scoring-engine
  newTag: "3.1.0"
