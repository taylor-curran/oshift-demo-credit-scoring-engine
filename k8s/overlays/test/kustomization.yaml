apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: credit-scoring-test

resources:
- ../../base

namePrefix: pe-eng-credit-scoring-test-

commonLabels:
  environment: test

patchesStrategicMerge:
- deployment-patch.yaml

images:
- name: registry.bank.internal/credit-scoring-engine
  newTag: "3.1.0-test"
