apiVersion: v1
kind: ConfigMap
metadata:
  name: pe-eng-credit-scoring-engine-postgres-primary-config
  labels:
    app.kubernetes.io/name: credit-scoring-engine
    app.kubernetes.io/version: "3.1.0"
    app.kubernetes.io/part-of: retail-banking
    environment: dev
    managed-by: helm
data:
  host: "postgres-primary-hostname"
  port: "5432"
  database: "credit_scoring"
  connection-params: "sslmode=require&application_name=credit-scoring-engine"
  max-connections: "20"
  connection-timeout: "30"
