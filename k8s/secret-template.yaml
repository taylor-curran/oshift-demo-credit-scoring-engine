# 
#
#

apiVersion: v1
kind: Secret
metadata:
  name: pe-eng-credit-scoring-engine-prod-secrets
  labels:
    app.kubernetes.io/name: credit-scoring-engine
    app.kubernetes.io/version: "3.1.0"
    app.kubernetes.io/part-of: retail-banking
    environment: prod
    managed-by: openshift
type: Opaque
stringData:
  DB_USERNAME: "REPLACE_WITH_ACTUAL_DB_USERNAME"
  DB_PASSWORD: "REPLACE_WITH_ACTUAL_DB_PASSWORD"
  REDIS_PASSWORD: "REPLACE_WITH_ACTUAL_REDIS_PASSWORD"
  EXPERIAN_API_KEY: "REPLACE_WITH_ACTUAL_EXPERIAN_API_KEY"
  EQUIFAX_API_KEY: "REPLACE_WITH_ACTUAL_EQUIFAX_API_KEY"
  TRANSUNION_API_KEY: "REPLACE_WITH_ACTUAL_TRANSUNION_API_KEY"
